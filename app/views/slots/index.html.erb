<p style="color: green"><%= notice %></p>

<h1>Slots</h1>

<div id="slots">
  <% @slots.each do |slot| %>
    <%= render slot %>
    <p>
      <%= link_to "Show this slot", doctor_slot_path(@doctor) %>
    </p>
  <% end %>
</div>

<%= link_to "New slot", new_doctor_slot_path %>
